spring.application.name=mySpringAi_MCP_Client
################ Log 相關設定 ################
logging.pattern.console=%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n
# 開啟 PrettyLoggerAdvisor 與 SimpleLoggerAdvisor 的 DEBUG 等級日誌，用來觀察 ChatClient 的 prompt/response 流程。
logging.level.com.example.myspringai_mcp_client.advisor.PrettyLoggerAdvisor=DEBUG
# logging.level.org.springframework.ai.chat.client.advisor.SimpleLoggerAdvisor=DEBUG
################ LLM model OpenAI 設定 ################
# OpenAI api key
spring.ai.openai.api-key=${OPENAI_API_KEY:} 
# OpenAI-model type
spring.ai.openai.chat.model=gpt-4o-mini
# ################ MCP tool filter 設定 ################
# blocked-servers：只要 MCP server 名稱包含以下任一關鍵字，就封鎖該 server 的所有 tools。example: github
# blocked-tool-prefixes：只要 tool 名稱以前綴開頭，就封鎖該 tool。example: write_
mcp.tool-filter.blocked-servers=
mcp.tool-filter.blocked-tool-prefixes=
################ MCP inspector 相關設定 ################
# npx @modelcontextprotocol/inspector
# 啟動一個本機 MCP Inspector UI
#   → 讓你連到 MCP Server
#   → 查看 server 提供哪些 tools/resources/prompts
#   → 手動測試 tool call
#   → 看 tool schema、輸入參數、執行結果
# ################ github 測試repo 設定 ################
# git@github.com:tonysk0210/mymcp.git
